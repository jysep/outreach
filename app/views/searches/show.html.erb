<h3 class="card-title"><%= link_to "< #{@campaign.name}", campaign_path(@campaign) %> Search</h3>
<div class="card-block">
	<%= link_to "Search", new_campaign_search_path(@campaign.id), class: "btn btn-primary" %>
</div>
<%= render "campaigns/entry_list", entries: @entries %>
